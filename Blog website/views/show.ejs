<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blog Posts</title>
    <style>
        /* Consistent Minimalist Base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
                         Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #fafafa;
        }
        
        /* Header & Navigation */
        header {
            padding: 1.5rem 2rem;
            border-bottom: 1px solid #eee;
        }
        
        nav {
            display: flex;
            justify-content: center;
            gap: 2rem;
        }
        
        a {
            color: #333;
            text-decoration: none;
            font-weight: 400;
            transition: color 0.2s ease;
        }
        
        a:hover {
            color: #666;
        }
        
        /* Main Content */
        main {
            max-width: 800px;
            margin: 3rem auto;
            padding: 0 2rem;
        }
        
        h1 {
            font-size: 2.5rem;
            font-weight: 300;
            color: #222;
            text-align: center;
            margin-bottom: 2rem;
        }
        
        /* Blog List Styling */
        .blog-list {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
        
        .blog-preview {
            padding: 1.5rem;
            border: 1px solid #eee;
            border-radius: 4px;
            background: white;
            transition: transform 0.2s ease;
        }
        
        .blog-preview:hover {
            transform: translateY(-3px);
        }
        
        .blog-preview h2 {
            font-size: 1.4rem;
            font-weight: 400;
            color: #222;
            margin-bottom: 0.5rem;
        }
        
        .snippet {
            color: #666;
            margin-bottom: 1rem;
            font-weight: 300;
        }
        
        /* Button Styling */
        .btn {
            display: inline-block;
            padding: 0.6rem 1.2rem;
            background: white;
            border: 1px solid #ddd;
            border-radius: 4px;
            color: #333;
            font-size: 0.9rem;
            transition: all 0.2s ease;
        }
        
        .btn:hover {
            border-color: #999;
            background-color: #f5f5f5;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            color: #666;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 600px) {
            nav {
                flex-direction: column;
                gap: 0.8rem;
                align-items: center;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            main {
                padding: 0 1.5rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            <a href="/add-blog">Add Blog</a>
            <a href="/show">View Blogs</a>
            <a href="/about">About</a>
        </nav>
    </header>

    <main>
        <h1>All Blog Posts</h1>
        
        <% if (blogs.length > 0) { %>
            <div class="blog-list">
                <% blogs.forEach(blog => { %>
                    <article class="blog-preview">
                        <h2><%= blog.title %></h2>
                        <p class="snippet"><%= blog.snippet %></p>
                        <a href="/show/<%= blog._id %>" class="btn">Read More</a>
                    </article>
                <% }); %>
            </div>
        <% } else { %>
            <p class="empty-state">No blog posts yet. <a href="/add-blog">Add one now!</a></p>
        <% } %>
    </main>
</body>
</html>