<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %></title>
    <style>
        /* Consistent Minimalist Base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
                         Helvetica, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #fafafa;
            padding: 0;
        }
        
        /* Header & Navigation */
        header {
            padding: 1.5rem 2rem;
            border-bottom: 1px solid #eee;
        }
        
        nav {
            display: flex;
            justify-content: center;
            gap: 2rem;
        }
        
        a {
            color: #333;
            text-decoration: none;
            font-weight: 400;
            transition: color 0.2s ease;
        }
        
        a:hover {
            color: #666;
        }
        
        /* Article Content */
        article {
            max-width: 700px;
            margin: 3rem auto;
            padding: 2rem;
            background: white;
            border: 1px solid #eee;
            border-radius: 4px;
        }
        
        h1 {
            font-size: 2rem;
            font-weight: 400;
            color: #222;
            margin-bottom: 1.5rem;
        }
        
        em {
            display: block;
            color: #666;
            font-style: normal;
            font-weight: 300;
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }
        
        div {
            color: #555;
            line-height: 1.7;
        }
        
        div p {
            margin-bottom: 1.5rem;
        }
        
        /* Back Link */
        .back-link {
            display: inline-block;
            margin-top: 2rem;
            padding: 0.6rem 1.2rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            color: #333;
            transition: all 0.2s ease;
        }
        
        .back-link:hover {
            border-color: #999;
            background-color: #f5f5f5;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 600px) {
            header {
                padding: 1.2rem 1.5rem;
            }
            
            nav {
                flex-direction: column;
                gap: 0.8rem;
                align-items: center;
            }
            
            article {
                margin: 2rem 1.5rem;
                padding: 1.5rem;
            }
            
            h1 {
                font-size: 1.7rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            <a href="/add-blog">Add Blog</a>
            <a href="/show">View Blogs</a>
            <a href="/about">About</a>
        </nav>
    </header>

    <article>
        <h1><%= blog.title %></h1>
        <p><em><%= blog.snippet %></em></p>
        <div><%= blog.body %></div>
        <a href="/show" class="back-link">← Back to All Posts</a>
    </article>
</body>
</html>